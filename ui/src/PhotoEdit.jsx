/* globals React */
import React from "react";

export default function PhotoEdit({ match }) {
    const { id } = match.params;
    //asdf
    return <div>{`This is a placholder for editing Photo id ${id}`}</div>;
}