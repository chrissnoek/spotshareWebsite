/* globals React */
import React from "react";

export default function WorkshopEdit({ match }) {
    const { id } = match.params;
    //asdf
    return <div>{`This is a placholder for editing Workshop id ${id}`}</div>;
}